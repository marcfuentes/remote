<%- model_class = Job -%>
<div class="row">
  <div class="span7 offset1">

</br>
   <h4><%= @job.category.name %></h4>

    </br>


  
  <h2><%= @job.title %></h2>
  <p><%= t("job.Publicado") %> <%= @job.created_at.to_date %>

  </p>
 
  </br>

  <h4><%= @job.description %></h4>
</div>

<div class="span3 offset1">
  </br>
  </br>
   </br>
  </br>
<%= image_tag(@job.logo_url, :width => "60%") if @job.logo.present? %>
<h3><%= @job.company %></h3>
<h4>Ubicación: <%= @job.headquaters %></h4>
 <h4><%= @job.url %></h4>

</div>
</div>

  
  

</br>
</br>

<div class="well ">
  <h3>Como postular</h3>

  <h4><%= @job.apply %></h4>

</div>

  
 

  
  
<% if can? :update, @job %>
<div class="form-actions">
  <%= link_to t('.back', :default => t("helpers.links.back")),
              jobs_path, :class => 'btn'  %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_job_path(@job), :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              job_path(@job),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
<% end %>

</div>
</div>
</br>
</br>